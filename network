pip install network matplotlib 
import network as nx 
import matplotlib.pyplot as plt 

G = nx.DiGraph()

G.add_edge(1879, 1884, weight=1.5)
G.add_edge(4079, 4080, weight=2.0)

print(nx.infor(G))

nx.draw(G, with_labels=True, node_size=10, front_size=8)
plt.show()
